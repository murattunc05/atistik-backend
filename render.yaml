services:
  - type: web
    name: atistik-api
    runtime: python
    buildCommand: apt-get update && apt-get install -y libgomp1 && pip install -r requirements.txt
    # Restore/write locks and readiness state are process-local. Keep a single
    # worker so two startup restores cannot race on the shared JSONL file.
    startCommand: gunicorn api_server:app --bind 0.0.0.0:$PORT --timeout 240 --workers 1
    envVars:
      - key: PYTHON_VERSION
        value: "3.11"
      - key: ATISTIK_RESTORE_TOKEN_SHA256
        value: a74dbe069f765fda3e15b635291507b6aacedd9ac2610630768910d6c6a652da
    plan: free
    healthCheckPath: /health
